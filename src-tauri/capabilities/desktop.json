{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-only",
  "description": "Launch-at-login. The autostart plugin is compiled only for desktop targets, so this capability is scoped to match.",
  "windows": [
    "main"
  ],
  "platforms": [
    "windows",
    "linux",
    "macOS"
  ],
  "permissions": [
    "autostart:default"
  ]
}
